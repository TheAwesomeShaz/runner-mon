using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    // Start is called before the first frame update
//     void Start()
//     {
//      // create the converter
// var converter = SMConverter.Blend(from, to);

// // setup conversion time
// converter.BlendTime = 2f;

// // add some handlers to get good blending effect
// converter.AddHandler(new SmcZeroPressHandler());
// converter.AddHandler(new SmcBumpDiffMaterialHandler(texture));   
//     }

//     // Update is called once per frame
//     void Update()
//     {
        
//     }
}
